# WooJob!!! 项目日报 & 未来开发计划

> 文档版本：v1.0
> 编写日期：2026-04-29
> 负责人：Vance

---

## 一、今日开发日报（2026-04-29）

### 1.1 今日完成事项

#### 环境配置问题排查
- **问题现象**：应用运行时报错 `Missing Supabase environment variables`，控制台不断出现 500 错误
- **根因分析**：
  - `.env.local` 文件可能缺少或未正确配置 `NEXT_PUBLIC_SUPABASE_URL` 和 `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - 服务器可能在配置环境变量之前就已启动
  - Next.js `.next` 缓存可能导致环境变量变更未生效
- **解决方案**：执行 `Remove-Item -Recurse -Force .next; npm run dev` 清除缓存后重启
- **状态**：⚠️ 待验证

#### 项目现状全面梳理
对项目进行了完整的代码审计，结果如下：

| 模块 | 完成度 | 说明 |
|------|--------|------|
| 前端 UI 组件 | ~95% | 看板、抽屉、日程视图、任务管理等核心组件全部完成 |
| 状态管理 | ✅ 100% | Zustand store 完整，含乐观更新和错误回滚 |
| Server Actions | ~90% | 全部 CRUD 操作已编写，类型完整，错误处理完善 |
| 数据库 | ✅ 100% | SQL schema 已文档化并已在 Supabase 执行，表结构/RLS/索引均已就绪 |
| 用户认证 | ❌ 0% | 无登录/注册页面，无 Auth 集成 |
| AI 功能 | ❌ 0% | AI 侧边栏为纯 Mock，无实际 LLM 调用 |
| 文件上传 | ❌ 0% | BottomShelf 的简历上传仅为占位符 |
| 项目文档 | ✅ 100% | PRD、API 规范、设计系统、后端开发指南齐全 |

### 1.2 今日待处理（阻塞项）

```
⚠️ P0 阻塞项：
1. ✅ Supabase 数据库已初始化 — SQL 已执行，表结构与代码同步
2. Auth 配置好邮箱与github — 所有 RLS 策略无法生效，数据无用户隔离
3. ✅ .env.local 已确认生效 — 应用正常运行无报错
```

### 1.3 当前代码文件清单

```
app/
├── page.tsx              ✅ 主页面（看板 + 日程切换、状态管理、拖拽集成）
├── layout.tsx            ✅ 根布局（字体加载、SEO metadata）
├── globals.css           ✅ 全局样式（CSS 变量、动画 keyframes）
└── actions/
    ├── jobs.ts           ✅ 岗位 CRUD + 软删除/恢复/永久删除
    ├── tasks.ts          ✅ 任务 CRUD + 完成状态切换
    ├── stats.ts          ✅ 统计数据聚合
    └── _helpers.ts       ✅ DB ↔ API 类型转换工具函数

components/
├── KanbanColumn.tsx      ✅ 拖拽列（6 阶段）
├── JobCard.tsx           ✅ 拖拽卡片（含进度条、删除按钮）
├── SideDrawer.tsx        ✅ 岗位详情抽屉（3 Tab、读写双态）
├── TaskDetails.tsx       ✅ 任务详情抽屉
├── TrashDrawer.tsx       ✅ 回收站抽屉
├── AgendaView.tsx        ✅ 日程视图（迷你日历 + 任务分组）
├── BottomShelf.tsx       ⚠️ 底部工作台（简历上传为占位符）
└── AISidebar.tsx        ❌ AI 助手（纯 Mock，无实际功能）

store/
└── useJobStore.ts        ✅ Zustand v5 store（所有 CRUD + 乐观更新）

types/
├── index.ts              ✅ UI 层类型（JobStage、Job、Task 等）
└── database.ts           ✅ DB 层类型（DbJob、DbTask 等）

lib/
├── supabase.ts           ✅ Supabase 客户端（env.local 生效）
├── mockData.ts           ✅ Mock 数据（开发阶段保底）
└── dateUtils.ts         ✅ 日期工具函数

docs/
├── prd.md                ✅ 产品需求文档
├── project-instructions.md  ✅ 开发规范（含 SQL schema）
├── API-spec.md           ✅ REST API 规范
├── backend-development.md    ✅ 后端开发指南
├── DESIGN-SYSTEM.md      ✅ 设计系统
└── future-24-hours-feature.md  ✅ 未来24小时功能规划
```

---

## 二、Supabase 初始化待办清单

> 根据 `docs/project-instructions.md` 和 `docs/backend-development.md`

### 2.1 阶段 0：Supabase 项目初始化（P0）✅ 已完成

**操作位置：** Supabase Dashboard → SQL Editor

> ⚠️ 注意：以下 SQL 已在 2026-04-29 执行完毕，表结构已与代码同步。
> 如需参考，请使用 `docs/project-instructions.md` 中的 SQL（第 192-305 行）。

需要依次执行以下 SQL：

```sql
-- 0. 启用 UUID 扩展
CREATE EXTENSION IF NOT EXISTS "pgcrypto";

-- 1. 创建 ENUM 类型
CREATE TYPE job_stage AS ENUM ('待投递', '已投递', '笔试中', '面试中', 'Offer', '已结束');
CREATE TYPE tag_type AS ENUM ('referral', 'round', 'interview_time', 'remaining');
CREATE TYPE task_type AS ENUM ('面试', '笔试', '待投递', '待办事项');

-- 2. 创建 Jobs 表
CREATE TABLE jobs (
  id            uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id       uuid        REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  company       text        NOT NULL,
  title         text        NOT NULL,
  stage         job_stage   NOT NULL DEFAULT '待投递',
  deadline      date,
  key_time      text,
  website       text,
  description   text,
  notes         text,
  progress      integer     DEFAULT 10 CHECK (progress >= 0 AND progress <= 100),
  created_at    timestamptz DEFAULT now(),
  updated_at    timestamptz DEFAULT now(),
  deleted_at    timestamptz
);

-- 自动更新 updated_at 触发器
CREATE OR REPLACE FUNCTION update_updated_at()
RETURNS TRIGGER AS $$
BEGIN NEW.updated_at = now(); RETURN NEW; END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER jobs_updated_at
  BEFORE UPDATE ON jobs
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

-- RLS
ALTER TABLE jobs ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own jobs" ON jobs FOR ALL
  USING (user_id = auth.uid() AND deleted_at IS NULL)
  WITH CHECK (user_id = auth.uid());
CREATE POLICY "Users view own trashed jobs" ON jobs FOR SELECT
  USING (user_id = auth.uid());

-- 3. 创建 Job Tags 表
CREATE TABLE job_tags (
  id          uuid      PRIMARY KEY DEFAULT gen_random_uuid(),
  job_id      uuid      REFERENCES jobs(id) ON DELETE CASCADE NOT NULL,
  tag_type    tag_type  NOT NULL,
  tag_value   text      NOT NULL
);
ALTER TABLE job_tags ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own job tags via jobs" ON job_tags FOR ALL
  USING (
    EXISTS (SELECT 1 FROM jobs WHERE jobs.id = job_tags.job_id AND jobs.user_id = auth.uid() AND jobs.deleted_at IS NULL)
  )
  WITH CHECK (
    EXISTS (SELECT 1 FROM jobs WHERE jobs.id = job_tags.job_id AND jobs.user_id = auth.uid())
  );

-- 4. 创建 Tasks 表
CREATE TABLE tasks (
  id               uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id          uuid        REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
  job_id           uuid        REFERENCES jobs(id) ON DELETE SET NULL,
  title            text        NOT NULL,
  company          text,
  task_date        date        NOT NULL,
  task_time        time,
  tag              task_type   NOT NULL DEFAULT '待办事项',
  round            text,
  meeting_link     text,
  resume_filename  text,
  notes            text,
  is_completed     boolean     DEFAULT false,
  created_at       timestamptz DEFAULT now(),
  updated_at       timestamptz DEFAULT now()
);

CREATE TRIGGER tasks_updated_at
  BEFORE UPDATE ON tasks
  FOR EACH ROW EXECUTE FUNCTION update_updated_at();

ALTER TABLE tasks ENABLE ROW LEVEL SECURITY;
CREATE POLICY "Users manage own tasks" ON tasks FOR ALL
  USING (user_id = auth.uid())
  WITH CHECK (user_id = auth.uid());

-- 5. 创建索引（提升查询性能）
CREATE INDEX idx_jobs_user_id    ON jobs(user_id) WHERE deleted_at IS NULL;
CREATE INDEX idx_jobs_stage      ON jobs(stage) WHERE deleted_at IS NULL;
CREATE INDEX idx_job_tags_job_id ON job_tags(job_id);
CREATE INDEX idx_tasks_user_date ON tasks(user_id, task_date);
CREATE INDEX idx_tasks_job_id    ON tasks(job_id);
```

> ✅ 数据库初始化已完成。表结构、RLS 策略、索引均已正确创建并与代码同步。

### 2.2 阶段 1：Supabase Auth 配置（P0）⏳ 进行中

**操作位置：** Supabase Dashboard → Authentication

#### 邮箱登录配置
1. 进入 **Authentication → Providers → Email**，启用 Email Provider
2. 关闭 **Confirm email**（开发阶段方便测试）
3. **URL Configuration**：
   - Site URL: `http://localhost:3000`
   - Redirect URLs: `http://localhost:3000/auth/callback`

#### OAuth 可选配置（GitHub）
1. 进入 **Authentication → Providers → GitHub**
2. 在 GitHub Developer Settings 创建 OAuth App
3. 获取 Client ID 和 Secret，填入 Supabase

### 2.3 Server Actions 用户上下文注入（P0）

当前所有 Server Actions 未注入 `user_id`。Auth 配置完成后需修改 `app/actions/_helpers.ts`：

```typescript
// 新增：获取当前用户
export async function getCurrentUser() {
  const { data: { user } } = await supabase.auth.getUser();
  return user;
}
```

然后在 `jobs.ts`、`tasks.ts` 中每个操作都注入 `user_id = (await getCurrentUser()).id`。

---

## 三、未来开发计划

### 3.1 第一阶段：核心功能收尾（预计 1-2 天）

| 任务 | 优先级 | 产出物 |
|------|--------|--------|
| Supabase 建表 + Auth 配置 | ✅ 已完成建表，Auth 待配置 | 数据库就绪，RLS 生效 |
| 登录/注册页面 | P0 | `/login` + `/register` |
| Server Actions 接入用户上下文 | P0 | 所有数据按用户隔离 |
| `.env.local` 验证生效 | P0 | 应用正常运行无报错 |
| 简历上传功能实现 | P1 | 简历文件存储到 Supabase Storage |

### 3.2 第二阶段：AI 功能开发（预计 2-3 周）

#### AI 工作流一：NLU 极速建档（最简单，先做）
- 用户输入/粘贴文本或截图 → AI 提取关键字段 → 预览确认 → 一键入库
- 技术方案：调用 DeepSeek API，实体抽取 JSON 结构化输出
- 前端改动：`AISidebar.tsx` 的 `handleSendMessage` 改为真实 API 调用

#### AI 工作流二：智能岗位发现
- 后端 Cron Job 每日抓取大厂官网 → 存入 Supabase pgvector
- 前端 RAG 检索：用户自然语言 → 向量检索 → 返回岗位卡片列表
- 技术方案：Supabase pgvector + DeepSeek Embedding

#### AI 工作流三：模拟面试
- 触发路径：岗位详情抽屉 → 【AI 模拟面试】按钮
- 上下文：JD（description 字段）+ 简历文本
- 多轮对话：保持上下文窗口，生成复盘报告存入 `notes` 字段

### 3.3 第三阶段：体验优化（持续迭代）

| 任务 | 优先级 | 说明 |
|------|--------|------|
| AgendaView 筛选功能修复 | P1 | 近期面试/笔试/已投递按钮实际过滤任务 |
| 响应式移动端适配 | P1 | 看板在手机上缩放/滚动体验 |
| 实时数据同步 | P2 | 多设备登录时 WebSocket 通知 |
| 微信/飞书推送通知 | P2 | 面试提醒、Offer 过期提醒 |
| 简历知识图谱 | P2 | 用户简历内容向量化，支持智能 JD 匹配 |

---

## 四、技术债务 & 改进建议

1. **`mockData.ts` 保留作为开发保底** — 在 Supabase 未配置时可通过环境变量切换到 mock 数据模式
2. **类型冗余** — `types/index.ts` 和 `types/database.ts` 有部分重叠字段，考虑合并
3. **AgendaView 筛选按钮** — 当前仅调用 `setSelectedDate(null)`，需要真正实现按 task type 过滤
4. **BottomShelf 简历上传** — 需要 Supabase Storage bucket + RLS 配置
5. **错误处理 UI** — 当前错误仅在控制台，建议在 UI 层加 toast 通知
6. **单元测试** — 目前 0 测试，建议从 Server Actions 开始覆盖

---

## 五、立即可执行的下一步

```
┌─────────────────────────────────────────────────────────┐
│  ✅ 第一步：Supabase 数据库初始化已完成                  │
│  表结构、RLS 策略、索引均已正确创建                      │
├─────────────────────────────────────────────────────────┤
│  第二步：Supabase Auth 配置                             │
│  登录/注册页面（app/login/page.tsx,                    │
│  app/register/page.tsx）                               │
├─────────────────────────────────────────────────────────┤
│  第三步：将 Server Actions 中的 user_id 注入改为        │
│  从 supabase.auth.getUser() 获取                        │
└─────────────────────────────────────────────────────────┘
```
